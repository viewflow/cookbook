import { LitElement, html, css } from 'lit'
import { customElement} from 'lit/decorators.js'

@customElement('vf-task-approve')
export class Approve extends LitElement {
  render() {
    return html`
      <div>Assign</div>
    `
  }

  static styles = css`
  `;
}
